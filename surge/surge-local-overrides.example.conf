# Copy beside your active Surge profile as surge-local-overrides.conf (gitignored if you name it that locally).
# In the main profile, uncomment:  #!include surge-local-overrides.conf
#
# Use for PROCESS-NAME routing, extra DOMAIN rules, or Host hacks — keeps generated templates clean.

[Host]
# example.local = 127.0.0.1

[Rule]
# PROCESS-NAME,curl,节点选择
# PROCESS-NAME,ssh,DIRECT
# DOMAIN-SUFFIX,internal.company,DIRECT
